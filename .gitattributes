# Generated files (codegen output) are collapsed in GitHub PR diffs and excluded
# from language stats. See AGENTS.md "Generated code".
#
# Entries are added as generators land; the file lives at the repo root and is
# never duplicated in a subdirectory.

packages/*/worker-configuration.d.ts linguist-generated=true
